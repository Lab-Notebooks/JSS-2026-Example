# CodeScribe loop for MCFM cleanup and consolidation.
#
# Run from the project root:
#   code-scribe loop -p dev/transformations/mcfm-cleanup/loop.toml -m <model>

[tools]
bash = ["jobrunner", "python3"]

[[chat.user]]
content = '''
Each round, follow the round algorithm in dev/transformations/mcfm-cleanup/current_plan.md and
the contract in dev/transformations/mcfm-cleanup/desired_spec.md. Read the plan first, then the
spec.

Authority: current_plan.md and desired_spec.md are human-owned — read, do not modify.
agent_log.md is the only file in dev/transformations/mcfm-cleanup/ that the AI may modify.
On conflict, the Spec governs.
'''
