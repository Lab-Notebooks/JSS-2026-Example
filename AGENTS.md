# Agent guide

**Ignore `README.md`** — it is written for humans setting up the demo.

Work from the Spec (`desired_spec.md`) and Plan (`current_plan.md`) of the
transformation you are pointed at, under `dev/transformations/<name>/`.
